Flask==2.3.3
pdfplumber==0.10.3
PyMuPDF==1.23.7
Werkzeug==2.3.7

Flask is the web framework.
pdfplumber and PyMuPDF are used for PDF parsing.
Werkzeug is a Flask dependency but sometimes needed explicitly in deployment environments.
